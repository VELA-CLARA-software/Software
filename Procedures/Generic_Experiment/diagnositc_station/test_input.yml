#%YAML 1.1
#
# header settings
header:
    project_name: my test project
    author: !!str djs
    version: !!float 0.1
    working_directory: !!str "D:\\VELA\\GIT Projects\\Software\\Procedures\\Generic_Experiment\\"  # a string, disambiguated by quotes
    log_file: !!str "gen_ex_test_log.txt"  # a string, disambiguated by quotes
#
# hardware Controller  settings
magnet:
    # machine_mode MUST BE, physical, virtual
    machine_area: !!str C2B
    # in the future hopefully we'll get physics units
    field_units: !!str seti
    machine_mode: !!str physical
# won't bother with RF for now
# llrf:
    # # machine_mode MUST BE, physical, virtual
    # machine_area: [rf_gun,
                   # rf_sl01]
    # # in the future hopefully we'll get physics units
    # phase_units: llrf
    # amp_units: llrf
    # machine_mode: !!str physical

screen:
    # machine_mode MUST BE, physical, virtual
    machine_area: !!str CLARA_PH1
    machine_mode: !!str physical
camera:
    # machine_mode MUST BE, physical, virtual
    machine_mode: !!str physical
shutter:
    # machine_mode MUST BE, physical, virtual
    machine_area: !!str PI_LASER
    machine_mode: !!str physical
#
# The epxeriment
set_1:
    magnet:
        # these need to be unique names???
        S02-QUAD1: !!float 0.2
        S02-QUAD2: !!float -0.2
    screen: 
        S02-YAG-03: !!str in
get_1:
    camera:
        S02-CAM-03: !!int 5

set_2:
    magnet:
        # these need to be unique names???
        S02-QUAD1: !!float 0.2
        S02-QUAD2: !!float -0.2
    screen: 
        S02-YAG-03: !!str in
get_2:
    camera:
        S02-CAM-03: !!int 5
