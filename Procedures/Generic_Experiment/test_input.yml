#%YAML 1.1
# global settings THIS MUST EXIST
global:
    project_name: my test project
    author: !!str djs
    version: !!float 0.1
    machine_mode: !!str physical
    working_directory: !!str "D:\\VELA\\GIT Projects\\Software\\Procedures\\Generic_Experiment\\"  # a string, disambiguated by quotes
    script_log_file: !!str "gen_ex_test_log.txt"  # a string, disambiguated by quotes

# hardware	settings
#
magnets:
    # machine_mode MUST BE, physical, virtual
    machine_area: !!str clara_ph1
    # in the future hopefully we'll get physics units
    field_units: si

llrf:
    # machine_mode MUST BE, physical, virtual
    machine_area: [rf_gun,
                   rf_sl01]
    # in the future hopefully we'll get physics units
    phase_units: llrf
    amp_units: llrf

screen:
    # machine_mode MUST BE, physical, virtual
    machine_area: clara_ph1
experiment:
    set_1:
        magnet:
			# these need to be unique names???
			S02-QUAD1: !!float 0.2
            S02-QUAD2: !!float -0.2
        llrf:
            clara_lrrg_phase: !!float 165
            clara_l01_phase: !!float 125
        screen: !!str S01
    get_1: s
